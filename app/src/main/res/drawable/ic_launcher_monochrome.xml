<!-- Android 13+ themed icons use the approved flower's complete alpha silhouette. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="76dp"
        android:height="76dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/kimi_launcher_foreground"
            android:gravity="fill"
            android:filter="true"
            android:tint="#000000" />
    </item>
</layer-list>
