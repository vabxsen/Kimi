# Copy to keystore.properties (gitignored) and fill in.
# storeFile is resolved relative to this repository root.
#
# Never commit the real file or the .jks it points at. Losing either means you can
# never publish an update to an app already on Play under this key.
storeFile=kimi-release.jks
storePassword=
keyAlias=kimi
keyPassword=
